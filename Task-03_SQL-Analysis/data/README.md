# Data Directory

This directory contains data-related artifacts for Task-03.

Structure:
- raw/       : Represents original CSV data (not committed)
- processed/ : Represents processed, analysis-ready data
- olist.db   : Final SQLite database used for analysis

The workflow followed:
Raw CSV → SQLite staging tables → Final analytical tables → SQL analysis
