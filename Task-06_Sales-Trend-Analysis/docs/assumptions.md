# Assumptions

The following assumptions were made during the analysis:

- Each row in the `orders` table represents a single completed order
- `amount` reflects total revenue per order
- `order_date` is the actual transaction date
- Currency is consistent across all records
- No cancelled or refunded orders are included
- Dataset is assumed to be clean and free from duplicates